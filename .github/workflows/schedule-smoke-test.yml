name: Schedule Smoke Test
on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Print timestamp
        run: |
          date -u
          echo "schedule smoke test"
